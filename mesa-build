#!/bin/bash
source $HOME/src/fdo/menv

if [ -z "$DEBUG" ]; then
  # -fomit-frame-pointer
  export CFLAGS='-O2 -s -DNDEBUG'
  export CXXFLAGS="$CFLAGS"
  export CPPFLAGS="$CFLAGS"
fi

cd $HOME/src/fdo/mesa && \
  PATH=/usr/lib/ccache:$PATH make -j8 $*
